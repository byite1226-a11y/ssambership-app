# 쌤버십 앱 현재 진행상황 (실측 대조 보고)

> 이번 작업은 **파악·보고까지만**입니다. 코드·색 수정, 라이브 연결은 하지 않았습니다.
> 대상 정본 레포는 **`byite1226-a11y/ssambership-app`** 이며, 작업 브랜치 `claude/new-session-9ina5z`는 `master`와 **동일 커밋(diff 없음)** 이라 S0~S12 전부 포함합니다.
> 아래 모든 상태·플래그·라인번호는 **실제 코드에서 직접 확인**한 값입니다. `HANDOFF.md`와 대조해 어긋나는 부분은 별도로 표시했습니다.

---

## 실측 요약 (내가 다시 확인한 값)

| 항목 | 내가 확인한 실측치 | 지시서/문서 값 | 비고 |
|---|---|---|---|
| `lib` .dart 파일 | **122개** | 122개 | 일치 |
| `lib` 코드 줄수 | **약 11,575줄** | 11,575줄 | 일치 |
| 테스트 파일 | **34개** | 34개 | 일치 |
| 테스트 케이스(`test`/`testWidgets`) | **121개** | 지시서 137개 / HANDOFF 121개 | **HANDOFF(121)가 정확.** 지시서의 137은 과다 |
| 네이티브 폴더 | `android/`·`ios/` 존재(추적됨) | 있음 | 일치 (커밋 `373fd79`에서 포함) |
| 커머스 폴더 | **없음** (cash·commission·commerce·payment·iq 디렉터리 0) | 없음 | 일치 |
| 구동 방식 | 키 없어도 구동되나 **더미 데이터 아님 = '빈 앱'(빈 상태/로그인 유도)** | 지시서 "더미 데이터로 바로 실행" | **부정확 정정** — D절 참고 |

**기능별 파일 수(실측):** question_room 30 · community 18 · mypage 11 · mentors 6 · notifications 4 · auth 3 · dev 3 · onboarding 1.

---

## A. 완성된 것 (S0~S12)

`HANDOFF.md §2`의 세션별 완성 주장을 실제 코드와 1:1 대조했습니다. **모두 구현 확인됨.**

| 세션 | 내용 | 실측 확인 위치 | 상태 |
|---|---|---|---|
| **S0** | 스캐폴드·모듈구조·라우팅·Supabase(로컬)·토큰/상수/과목매핑 | `lib/main.dart`, `lib/app/router.dart`, `lib/core/config/app_config.dart`, `lib/data/mappings/subject_labels.dart` | ✅ 완성 |
| **S1** | 공통 위젯 + dev 갤러리 | `lib/design/widgets/*`(app_card·initial_avatar·app_badge·status_pill·primary/secondary_button·chip_scroll·empty_state 등 13개), `lib/features/dev/widget_gallery.dart` | ✅ 완성 |
| **S2** | 이메일 로그인·세션·역할·계정상태·게스트·마이페이지 로그아웃 | `lib/core/auth/auth_service.dart`(`AppRole{student,mentor,admin,guest}`, `AccessState{loading,loggedOut,guest,full,blocked}`), `lib/features/auth/login_screen.dart` | ✅ 완성 |
| **S3** | 질문방 데이터 계층(모델·레포·RLS) | `lib/features/question_room/data/models/*`, `question_room_read/write_repository.dart` | ✅ 완성 |
| **S4** | 학생 질문방 화면(목록→방홈→질문→채팅→연결노트) | `lib/features/question_room/ui/`(question_list·mentor_room_home·chat·connection_notes 등) | ✅ 완성 |
| **S5** | 멘토 질문방 화면 | `lib/features/question_room/ui/mentor/`(mentor_inbox·student_room_home·mentor_question_list·mentor_answer) | ✅ 완성 |
| **S6** | 실시간·이미지첨부·연결노트 저장(있으면 연결/없으면 골격) | `thread_realtime.dart`(Realtime), `attachments/attachment_upload.dart`(첨부 골격) | ✅ 완성(단, 인프라 게이트 OFF — B절) |
| **S7** | 푸시 인프라 클라이언트 골격 | `lib/core/push/`(push_service·device_token_registrar·edge_function_push_sender·push_trigger 등 7개) | ✅ 골격 완성(게이트 OFF — B절) |
| **S8** | 알림 센터(목록·읽음·유형필터·딥링크, CR/환불 제외) | `lib/features/notifications/`(app_notification.dart: CR·환불·IQ를 `NotificationKind.other`로 숨김) | ✅ 완성 |
| **S9** | 커뮤니티 열람·댓글(숏폼/게시판/내활동, 작성=웹) | `lib/features/community/`(18파일: shortform·board·activity·widgets) | ✅ 완성 |
| **S10** | 멘토 찾기(열람·상세, 구독=웹) | `lib/features/mentors/`(mentors_screen·mentor_detail·mentor_card) | ✅ 완성 |
| **S11** | 마이페이지 보강(구독현황·캐시조회·설정, 결제=웹) | `lib/features/mypage/`(11파일: cash·student_subscription·mentor_dashboard·settings·profile·support 섹션) | ✅ 완성. **S11 커밋의 "router 등록 TODO"는 해소됨** — `home_shell.dart:33`에서 `MyPageScreen()` 5탭에 정상 연결 |
| **S12** | 웹 브릿지 통일(URL 상수화, 미확정 시 폴백) | `lib/core/web_bridge/`(web_bridge·web_bridge_actions·web_bridge_config) | ✅ 완성(baseUrl 미확정 — B절) |

**하단 5탭 전부 구현 확인:** `lib/app/home_shell.dart:28-34` — 질문방·커뮤니티·멘토찾기·알림·마이페이지. 게스트는 멘토찾기(탭2)에서 시작, 보호 탭 진입 시 로그인 유도(`_onSelect`, line 67-73).

**기능/역할 하드룰 확인:**
- **커머스제로:** 앱 내 결제/가격입력 화면 0. 모든 결제 동선은 `web_bridge_actions.dart`의 `openSubscribeWeb`/`openRechargeWeb`/`openBillingManageWeb`/`openPayoutManageWeb`/`openProfileEditWeb`만 호출 → 웹만 엶. ✅ 지켜짐
- **관리자 차단:** `auth_service.dart:76` admin → `AccessState.blocked`(학생·멘토 전용). ✅
- **내부데이터 비노출:** 과목·상태는 한글 매핑(`subject_labels.dart`, `question_room_labels.dart`), 알림 type 원문 비노출(`app_notification.dart`). ✅
- **dev 도구 출시 제외:** `dev_flags.dart` `kDevToolsEnabled = !kReleaseMode`. ✅

---

## B. 남은 작업 (우선순위 · 코드위치 · 게이트)

각 게이트는 **플래그/상수 한 곳**만 바꾸면 켜지도록 설계돼 있습니다(로직은 미리 작성됨). 인프라 생성은 이 앱 범위 밖.

### ① 색 확정 적용  ⬅ 다음 지시 대상
- **위치:** `lib/design/tokens/color_tokens.dart` (역할 구조 유지, hex만 교체)
- **현재:** 전부 placeholder. **다크 테마 + 스카이 강조** 조합.
  - 표면: `page #0B1220` / `surface #111A2B` / `elevated #1A2740`
  - 전경: `primary #F1F5F9` / `secondary #94A3B8` / `muted #64748B`
  - 강조: `accent #38BDF8` / `accentMuted #0EA5E9` (스카이 계열)
  - 상태: `success #22C55E` / `warning #F59E0B` / `danger #EF4444`, 경계 `border #233047`
- **주의:** 확정색이 **파랑 `#2563EB` + 은은한 그라데이션**이므로 현재 스카이(`#38BDF8`)와 다름 → D절 갭 참고. **통째 교체 금지, hex만.**

### ② 잠금값(플랜 라벨·주간질문수)
- **위치:** `lib/shared/constants/plan_constants.dart`
- **현재(전부 비어 있음):**
  - `PlanTier{limited, standard, premium}` enum (line 9) — 존재
  - `planLabels` (line 12-16): 세 값 모두 `''` (TODO)
  - `planMonthlyPriceCash` (line 20-24): 세 값 모두 `null`
  - `planWeeklyQuestionQuota` (line 27-31): 세 값 모두 `null` (프리미엄은 FUP)
- **연동:** `lib/core/entitlement/subscription_summary.dart` — `remaining`이 현재 `null`(line 57), 게이팅은 `isActive`로만(`canAsk`, line 26). 주간 4·9·19 확정 시 여기 채우면 활성.
- **하면:** 구독 요약에 잔여 문항수 표시, 라벨 노출.

### ③ 가입·인증 (신규 — 코드에 아직 없음)  ⬅ D절 갭
- **현재:** 앱 내 **회원가입 폼 없음**. 로그인 화면에 "웹에서 가입" 안내만(`login_screen.dart:72 _openWebSignUp`, line 76 `'회원가입은 웹에서 진행돼요. (링크 준비 중)'`).
- **인증:** 이메일+비밀번호만(`auth_service.dart:190 signInWithPassword`). **PASS/본인인증·만14 연령게이트 코드 전무**(전체 검색 결과 0건).
- **결정과의 차이:** 우리 결정 = "앱 가입 + PASS 인앱웹뷰 + 만14 연령게이트" → **전부 신규 구현 항목.** (기존 골격 없음, 새 화면·플로우 설계 필요)

### ④ 라이브 연결 (인프라 게이트 — 전부 OFF)
| 대상 | 플래그/상수 | 파일:라인 | 현재값 | 켜는 조건 |
|---|---|---|---|---|
| **Supabase 실데이터** | `.env`의 `SUPABASE_URL`/`SUPABASE_ANON_KEY` | `lib/core/config/app_config.dart`, `.env.example` | 로컬(`127.0.0.1:54321`) | 원격 production 값 주입 (`hasCredentials`) |
| **웹 URL(결제 동선)** | `baseUrl` | `web_bridge_config.dart:14` | `''`(빈값) → 폴백 안내 | 운영 도메인 입력 → `isConfigured`(line 24) true. 경로 5종 line 17-21 |
| **Storage(이미지첨부)** | `_storageReady` | `attachment_upload.dart:96` | `false` | 버킷 `question-attachments`(line 93) + 정책 생성 후 true. + `image_picker` 도입해 `DisabledImagePicker`(line 61) 교체 |
| **Realtime** | (publication 포함 여부) | `thread_realtime.dart`(SupabaseThreadRealtime) | 코드 완성, 폴백 동작 중 | `question_messages`·`question_threads`를 `supabase_realtime` publication에 추가. 미포함이어도 수동 새로고침 폴백(`chat_screen.dart:109 _refresh`, 버튼 line 193-196) |
| **FCM device_tokens** | `_tableExists` | `device_token_registrar.dart:13` | `false` | `device_tokens` 테이블 DDL 후 true |
| **FCM Edge Function** | `_deployed` | `edge_function_push_sender.dart:17` | `false` | `send-push`(functionName line 14) 배포 후 true |
| **FCM 발송 트리거** | (미연결) | `push_trigger.dart` | 메서드만 존재 | `onMentorAnswered`/`onNewQuestionForMentor`/`onNewMessageForStudent`를 질문방 이벤트 성공 직후 호출 |
| **Firebase 도입** | (미도입) | `pubspec.yaml` | `firebase_core`·`firebase_messaging` 없음 / `image_picker` 없음 | 패키지 추가 + `flutterfire configure` + 플랫폼 설정 |

> **현재 pubspec 의존성(실측):** `supabase_flutter ^2.5.0`, `go_router ^14.2.0`, `flutter_dotenv ^5.1.0`, `url_launcher ^6.3.0`. (firebase·image_picker **없음** — 위 게이트와 일치)

### ⑤ 빌드·출시
- 네이티브 폴더 존재(`android/`·`ios/`). 필요 시 `flutter create . --org com.ssambership …`로 누락분만 생성.
- `.env` 원격 전환(위 ④ Supabase), Android 릴리스 서명·Play Store / iOS 번들ID·서명·App Store 등록.
- FCM은 실기기 검증 필요(에뮬레이터 제한).

---

## C. 재베이스라인 (기존 세션계획 → 실물 상태 재정렬)

기존 "빌드 세션(S0~S17)" 전제는 폐기합니다. **앱은 이미 S0~S12까지 구현·테스트 완료** 상태이므로, 남은 일은 '빌드'가 아니라 **'마감 + 리테마 + 연결'** 입니다.

| 구분 | 기존 계획 취급 | 재정렬 후 |
|---|---|---|
| S0~S12 (5탭·데이터계층·골격) | 빌드 대상 | **완료 처리** (A절) |
| 색 확정 적용 | 미정 | **마감 작업 1** (B①) — 다음 지시 |
| 잠금값(라벨·문항수) | 미정 | **마감 작업 2** (B②) — 값 확정 시 |
| 가입·PASS·연령게이트 | 계획에 없던 신규 | **신규 개발** (B③/D) — 골격 없음, 설계부터 |
| 라이브 연결(5게이트) | "연결" 단계 | **연결 작업** (B④) — 플래그 토글 중심 |
| 빌드·출시 | 최종 | **출시 작업** (B⑤) |

**권장 순서:** ① 색 → ② 잠금값 → ④ 웹URL·Supabase(즉효 게이트) → ③ 가입·인증(신규, 공수 큼) → ④ Storage·Realtime·FCM → ⑤ 출시.

---

## D. 코드 ↔ 결정 갭

| # | 결정/문서 | 현재 코드 | 판정 |
|---|---|---|---|
| D1 | 가입 = "앱 가입 + PASS 인앱웹뷰 + 만14 연령게이트" | 가입=웹 안내만(`login_screen.dart:76`), PASS·연령게이트 **코드 0** | **신규 작업** — 기존 골격 없음. 화면·플로우 신설 필요 |
| D2 | 확정색 = 파랑 `#2563EB` + 은은한 그라데이션 | 토큰이 **다크 + 스카이(`#38BDF8`)** placeholder | **리테마 필요** — 역할 구조 유지·hex 교체(다음 지시). 다크/라이트 방향도 함께 확정 요망 |
| D3 | 지시서 실측 "더미 데이터로 바로 실행됨" | 키 없으면 **'빈 앱'**(빈 상태/로그인 유도)로 구동, **더미 데이터 아님**(`main.dart` 주석 "빈 앱만 구동"). 데모용 가짜 데이터 소스 없음 | **실측 정정** — "키 주입 시 실데이터 전환/화면코드 동일"은 맞음. 단 '더미'가 아니라 '빈 상태' |
| D4 | 지시서 테스트 "137개" | 실제 `test`/`testWidgets` **121개** (34파일) | **수치 정정** — HANDOFF(121)가 정확 |
| D5 | 잠금값(주간질문 4·9·19, 플랜 라벨) | `plan_constants.dart` 전부 `''`/`null` | **값 대기** — 구조는 준비됨, 확정값만 주입 |
| D6 | 결제 동선 "웹으로만" | `web_bridge_config.dart baseUrl=''` → 실제로는 "준비 중" 폴백만 뜸 | **정합** — 결정과 일치. baseUrl 확정 전까지 웹 열기 자체가 미동작(의도된 폴백) |

---

## 경계 준수 확인

- ✅ 코드·색 **미수정** (이 문서만 추가).
- ✅ 라이브 연결 **미실행** (더미/빈 상태 유지, 플래그 그대로).
- ✅ 정본 레포(`ssambership-app`)만 대상. `ssambership_flutter` 무시.
